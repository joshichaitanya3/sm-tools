# sm_tools

Functions we reuse for self-assembly analysis: pair potentials,
periodic-boundary distances, and a couple of observables.
Everything lives in `sm_tools.py`.

```python
from sm_tools import lennard_jones, pair_distance
```
